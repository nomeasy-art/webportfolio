media:
  input: images/projects
  output: images/projects
  extensions: [jpg, jpeg, png, webp, gif, svg, mp4, webm]

content:
  - name: projects
    label: Progetti
    type: collection
    path: projects
    format: json
    filename: "{fields.slug}.json"
    fields:
      - name: title
        label: Titolo
        type: string
        required: true
      - name: slug
        label: Slug
        type: string
        required: true
      - name: collaborator
        label: Collaboratore
        type: string
      - name: year
        label: Collaboratori
        type: string
      - name: description
        label: Descrizione
        type: text
      - name: category
        label: Categoria
        type: select
        required: true
        options:
          values:
            - name: editorial
              label: Editorial
            - name: packaging
              label: Packaging
            - name: book-cover
              label: Book Cover
            - name: user-interface
              label: Brand Identity
      - name: tags
        label: Tag
        type: string
        list: true
      - name: mainImageUrl
        label: Immagine o Video Principale
        type: file
        options:
          extensions: [jpg, jpeg, png, webp, gif, mp4, webm]
      - name: gallery
        label: Galleria (Immagini e Video)
        type: file
        options:
          multiple:
            max: 20
          unique: true
          extensions: [jpg, jpeg, png, webp, gif, mp4, webm]
      - name: externalLink
        label: Link Esterno
        type: string
      - name: featured
        label: In Evidenza
        type: boolean
      - name: order
        label: Ordine
        type: number
    view:
      fields: [mainImageUrl, title, category, year, featured]
      primary: title
      sort: [order, title, year]
      search: [title, description, collaborator, tags]
      default:
        sort: order
        order: asc
